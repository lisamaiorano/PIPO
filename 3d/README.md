# 3D Files

Open these files in OpenSCAD.

- `pipo_access_card.scad`: full access card with front text and back QR placeholder.
- `qr_plate.scad`: standalone QR placeholder plate for testing sticker size.

The QR area points conceptually to `GAME_URL_PLACEHOLDER`. Replace it with a real generated QR code after publishing the game.
